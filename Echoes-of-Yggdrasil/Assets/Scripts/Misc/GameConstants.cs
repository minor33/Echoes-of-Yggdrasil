using UnityEngine;


public static class GameConstants {
    
}

public enum Target {
    CHOOSE,
    RANDOM,
    FRONT,
    SELF
}

public enum Keyword {
    DAMAGE,
    BLOCK,
    TARGET,
    DRAW
}

public enum EnemyAction {
        ATTACK,
        DEFEND,
        PAUSE
}
